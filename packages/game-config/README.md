# game-config

共享游戏配置目录。

适合存放：

- 关卡配置
- 分数规则
- 连击阈值
- 文案
- 小鸡皮肤配置
- 活动配置

建议后续把可调参数从代码里抽出来，统一放到这里。

## 当前内容

- `configs/gameplay.v1.json`：第一版玩法配置样例

## 建议做法

- 本地放一份默认配置
- 客户端启动时优先读取本地配置
- 后端配置下发后再做覆盖

这样前期可以脱离后端独立调手感，后面再把配置托管到服务端。
